package com.interpreterNodes;

/**
 * Marker interface for all AST node types.
 * Every node in the Abstract Syntax Tree implements this interface,
 * allowing the interpreter to handle all node types uniformly via evaluate().
 */
public interface InterpreterNode {
}