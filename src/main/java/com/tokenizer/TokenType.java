package com.tokenizer;

/**
 * All token types recognized by the Tokenizer.
 */
public enum TokenType {VAR, ASSIGN, LPAR, RPAR, PLUS, MULT, VAL, IF, THEN, ELSE, GT,LT,EQ,LT_EQ,GT_EQ, NOT_EQ, WHILE, DO, FUN, RETURN, LBRACE, RBRACE, MINUS, TRUE, FALSE, DIV, COMMA}
